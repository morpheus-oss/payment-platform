package io.morpheus.payments.common.error;

public enum ErrorCode {
    WALLET_NOT_FOUND,
    INSUFFICIENT_FUNDS,
    INVALID_TRANSFER,
    DUPLICATE_TRANSACTION,
    FRAUD_DETECTED,
    INTERNAL_ERROR
}